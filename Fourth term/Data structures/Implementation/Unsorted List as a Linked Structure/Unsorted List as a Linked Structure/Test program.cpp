#include<iostream>
#include"UnsortedType.h"
using namespace std; 
int main() {

	UnsortedType<int> list; 
	list.InsertItem(20);
	list.InsertItem(20);
	list.InsertItem(20);
	list.InsertItem(20);

	return 0; 
}